import { IorganizerRepository } from "../../interface/repositoryInterface/organizerRepository";


export const createOrganizer = async( OrganizerRepository:IorganizerRepository , name: string,email:string,password:string,country: string, state:string, city:string,picode:number , ownerId: string ,companyLicense: string,companyInsurance:string, bankPassbook:string, building:string) : Promise <string | void > =>{
    // const address = await 
    // const createOrganizer = await OrganizerRepository.createOrganizer()
} 

